# BXH ARENA v13.29.4 — 封測候選版

以 v13.29.3 為基線，保留既有 HTML、Firebase Auth、Firestore 與 GitHub Pages 部署方式。本次只製作交付檔案，未連接或部署使用者的正式 Firebase 專案。

## 更新

- 最高管理員的「稱號與每日簽到」新增連線診斷：前端專案、區域、登入、SDK 是否就緒，以及可信後端版本／Firestore 讀取結果。失敗時提供錯誤分類；單一錯誤不能證明後端未部署。
- 本月簽到日曆從後端讀取當月紀錄（最多 31 筆），不是用手機日期生成簽到成功紀錄。
- 依伺服器時間安排午夜讀取；回到可見頁面時也重新確認。離線失敗仍需手動重試，不會自動補簽。
- 跨月本月簽到數歸零；最後簽到早於昨日時，目前連續天數顯示 0，歷史最高及累積不變。
- 簽到交易提交成功後，即使稱號整體判定失敗仍回報成功；再次簽到請求不會重複計數，並可重試稱號判定。這不是新增自動補發排程。
- 已確認的簽到／稱號設定結果在後續讀取失敗時保留。稱號未儲存草稿在同帳號重繪時保留，登出即清除。
- 所有稱號／簽到非同步操作加入帳號世代驗證；登出／切換帳號時清除新模組快取、草稿、診斷與補算游標。
- 稱號／簽到渲染例外限縮在模組區塊；不讓該例外中斷賽事大廳繪製。
- 初始化只建立缺少的稱號定義；既有定義與建立時間不覆蓋，公開目錄依現有定義同步。
- 儲存功能開關不再清空既有 launchAt。
- 補算新增統計、已處理、補發稱號、跳過與失敗分開計數。部分失敗保留原游標、允許同批重試。每次執行使用獨立識別碼。
- 簽到排行標示測試帳號；排行依歷史最高連續天數排序，包含測試帳號，不等於純正式排行榜。

## 未改動與限制

- Rules 權限內容與 v13.29.3 完全一致，只有版本註解變更。
- 沒有清除或重建既有 tournaments、天梯資料、簽到紀錄或已取得稱號。
- 開拓者前 100 名保持原有停用預設；本版不設定正式開服時間、不啟用名額發放。
- 每日彙總仍從 v13.29.3 部署後累積，不代表過往全部簽到；近 7 日／本月是人次。異常掃描最多 250 筆，不是全庫稽核。
- 補算仍只涵蓋能核對既有官方積分賽天梯交易的資料。UI 先預覽再執行；後端每次重新驗證，不是鎖定歷史資料的快照。
- 本版沒有新增補簽、獎品、貨幣或進階稱號條件。

## 已執行驗證

- 兩個 HTML 內嵌 script 完整語法檢查（含 module）。
- Functions 語法與本機套件載入，共 12 支匯出函式。
- test_behavior.cjs：在記憶體資料庫替身上測試初始化保留／冪等、交易先讀後寫、日切月切、重複簽到、提交後稱號失敗、部分補算失敗／重試。
- 前端 VM 行為測試：讀取失敗不循環、閏年日曆、登出清除、A 帳號延遲回應不能覆寫 B。
- 既有登入／Session 與帳號分類回歸（帳號測試僅補上新渲染包裝的測試替身）。
- npm audit --omit=dev：本次執行 0 項弱點。

上述測試替身不是 Firestore Emulator、真實交易競爭或瀏覽器實機測試。環境使用 Node 24，本版部署設定仍沿用 Node 20，需在部署端補做對應 runtime 驗證。

## 未完成驗收

- Firestore Emulator：啟動時網路核准取消，已停止；未通過，不宣稱 Rules 或完整角色權限已實測。
- iPhone／Android／桌面真實瀏覽器驗證：待測。
- 正式 Firebase Rules／Functions 部署、真實帳號連線：未執行。

完整雲端與實機驗收前，請保持封測，不要視為全面上線通過。

## 部署順序

1. 備份當前網站檔案，確認 Firebase CLI 顯示的是 BXH ARENA 正確專案。
2. 在解壓縮目錄執行 `npm --prefix functions ci` 與 `npm --prefix functions run check`。
3. 在部署端補跑 Emulator 與角色／交易測試。包內 `emulator.json` 與 `test_emulator.cjs` 只提供編譯與匿名讀寫六項起始檢查，不是完整測試。
4. 發布本版 Rules，再部署 Functions，最後更新 index.html。
5. 最高管理員登入，前往「帳號管理 → 稱號與每日簽到 → 檢查稱號與簽到連線」。確認專案、區域、後端 13.29.4 一致。
6. 先只開啟每日簽到與稱號介面；用封測帳號測試同日連點、斷網、登出再登入、稱號保存、跨日及日曆。
7. 正式測試登入、報名、報到、裁判、計分、天梯及封存；補算先預覽小批再執行。

本地測試：`node test_behavior.cjs .`。
Emulator 起始檢查：`firebase emulators:exec --project demo-bxh-v13294 --only firestore --config emulator.json "node test_emulator.cjs"`。

## 停止與回復

停用新功能開關，停止人工補算；保留新增紀錄。必要時可還原 v13.29.3 前端，但不可藉由刪除資料回復。若稱號日曆回到舊版本，後端新增回應欄位不會改變原始簽到紀錄。
